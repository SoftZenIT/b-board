// Placeholder to satisfy Vitest's file discovery requirement.
// Remove this file once real unit tests are added.
describe('project setup', () => {
  it('is ready', () => {
    expect(true).toBe(true);
  });
});
